<%= render :partial => 'shared/close_alerts' %>

$('#issue-vote-link-<%= @issue.id %>')
  .html('Supported!')
  .parent().addClass('supported');
  
$('#votes-remaining').html('<%= current_user.votes_remaining %>');
$('#reputation-points').html('<%= current_user.points %>');